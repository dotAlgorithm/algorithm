#include <bits/stdc++.h>

using namespace std;

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	
	int T, i, a, b;
	cin >> T;
	
	for(i=0; i<T; i++) {
		cin >> a >> b;
		cout << a+b << "\n";
	}
	return 0;
}
